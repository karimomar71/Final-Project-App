apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  labels:
    app: nodejs-demo-app
spec:
  type: LoadBalancer
  selector:
    name: nodjs-pod
    app: nodjs-demo-app
  ports:
  - port: 80
    targetPort: 80
